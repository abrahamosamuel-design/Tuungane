import fetch from 'node-fetch';

async function run() {
  const token = 'fake-token-just-for-test'; // I don't have the user token, so I can't really hit the auth endpoint.
  // wait, I can just tell the user the actual issue.
}
